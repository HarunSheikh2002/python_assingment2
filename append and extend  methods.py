# Differentiate between append () and extend () methods?

# 1) append( ) : using of append() method we can add element at end of the list

# 2) extend()  : using of extend() method we can add multiple element in existing list in the from of list format